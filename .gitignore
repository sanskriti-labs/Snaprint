# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*

# local
/.venv/
# reports
reports/
!reports/.gitkeep

# python
__pycache__/
*.pyc

# scraper raw output — large intermediate files, regenerate via the scraper.
# shops-clean*.jsonl is NOT ignored: content/pseo/shops.ts reads it at build time.
# *-xeroxshops.txt is NOT ignored: small hand-edited query config, not
# generated output — 4 of 6 cities' copies existed only on a scrape
# machine and were never in git until 2026-08-14.
scripts/scraper/data/results*.json
scripts/scraper/data/raw-pulls/
scripts/clean/data/llm-audit-cache.json

# claude code local config
/.claude/

# strix security scan output
/strix_runs/

# third-party tooling cache, not app code
/vendor/

# local run-state for scripts/seo/indexnow-submit.mjs (last-submitted lastmod per URL)
/.indexnow-state.json
